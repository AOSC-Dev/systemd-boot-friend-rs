#compdef systemd-boot-friend-rs

autoload -U is-at-least

_systemd-boot-friend-rs() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" \
'-h[Prints help information]' \
'--help[Prints help information]' \
'-V[Prints version information]' \
'--version[Prints version information]' \
":: :_systemd-boot-friend-rs_commands" \
"*::: :->systemd-boot-friend-rs" \
&& ret=0
    case $state in
    (systemd-boot-friend-rs)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:systemd-boot-friend-rs-command-$line[1]:"
        case $line[1] in
            (init)
_arguments "${_arguments_options[@]}" \
'-h[Prints help information]' \
'--help[Prints help information]' \
'-V[Prints version information]' \
'--version[Prints version information]' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" \
'-h[Prints help information]' \
'--help[Prints help information]' \
'-V[Prints version information]' \
'--version[Prints version information]' \
&& ret=0
;;
(install-kernel)
_arguments "${_arguments_options[@]}" \
'-h[Prints help information]' \
'--help[Prints help information]' \
'-V[Prints version information]' \
'--version[Prints version information]' \
'::target -- Target kernel in the list or the number of the kernel:_files' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'-h[Prints help information]' \
'--help[Prints help information]' \
'-V[Prints version information]' \
'--version[Prints version information]' \
&& ret=0
;;
        esac
    ;;
esac
}

(( $+functions[_systemd-boot-friend-rs_commands] )) ||
_systemd-boot-friend-rs_commands() {
    local commands; commands=(
        "init:Initialize systemd-boot and install the newest kernel" \
"list:List available kernels" \
"install-kernel:Install specific version of kernel" \
"help:Prints this message or the help of the given subcommand(s)" \
    )
    _describe -t commands 'systemd-boot-friend-rs commands' commands "$@"
}
(( $+functions[_systemd-boot-friend-rs__help_commands] )) ||
_systemd-boot-friend-rs__help_commands() {
    local commands; commands=(
        
    )
    _describe -t commands 'systemd-boot-friend-rs help commands' commands "$@"
}
(( $+functions[_systemd-boot-friend-rs__init_commands] )) ||
_systemd-boot-friend-rs__init_commands() {
    local commands; commands=(
        
    )
    _describe -t commands 'systemd-boot-friend-rs init commands' commands "$@"
}
(( $+functions[_systemd-boot-friend-rs__install-kernel_commands] )) ||
_systemd-boot-friend-rs__install-kernel_commands() {
    local commands; commands=(
        
    )
    _describe -t commands 'systemd-boot-friend-rs install-kernel commands' commands "$@"
}
(( $+functions[_systemd-boot-friend-rs__list_commands] )) ||
_systemd-boot-friend-rs__list_commands() {
    local commands; commands=(
        
    )
    _describe -t commands 'systemd-boot-friend-rs list commands' commands "$@"
}

_systemd-boot-friend-rs "$@"